# 资源删除约束由 Store 统一执行

机场来源、节点、模板和规则方案的删除约束由 Store 在同一个 bbolt 写事务中统一执行：内建资源和机场来源节点受保护，任何仍被输出订阅引用的资源都拒绝删除，停用、过期或生命周期阻断不会解除引用；冲突返回包含全部引用者的结构化错误，接口层只负责映射响应。删除检查使用事务内从现有输出订阅、节点和模板版本构造的临时引用图，不维护持久反向索引；允许删除时仍递增全局输出输入版本，使并发的旧编译结果不能在资源删除后提交。
