# TUI 实时数据采用事件和分区增量接口

Runtime 的状态、来源、网络、更新和操作结果继续以 Snapshot 为权威数据，并通过事件通知客户端重新读取；流量历史、活动连接和日志分别使用有界的增量接口，只有相关页面可见时才请求明细。IPC 中断时，TUI 保留最后一次数据并明确标为过期，按退避策略重连，恢复后先读取完整 Snapshot；某一实时区域读取失败时不影响其他页面操作。这样可以避免每秒重复传输完整 Snapshot 和大列表，同时保留一致的状态来源以及可控的资源占用。
