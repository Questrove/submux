# 流量策略支持本机持久覆盖

Submux Runtime 默认让流量策略跟随 Runtime 配置来源；操作员明确选择规则、全局或直连后，Runtime 将选择保存为本机设置，并在生成候选配置时优先于来源和本机高级覆盖。只修改当前 Mihomo 会使来源刷新或核心重启后状态悄然回退，而无条件覆盖来源又会改变现有配置含义，因此保留“跟随来源”作为默认和可恢复选项；全局策略引用的策略组不存在时拒绝生成候选配置。
