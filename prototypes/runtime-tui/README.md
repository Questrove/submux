# Runtime TUI 结构原型

这个目录用于回答一个设计问题：Runtime TUI 应该按什么方式组织，才能让常用操作容易发现，同时保留完整的管理能力？

这是只读、一次性的浏览器原型，不连接 Runtime，也不执行任何运行操作。前三个页面保留早期方案，`?variant=D` 展示已经确认、将在 #26–#41 中实现的组合方案。

在本目录启动静态文件服务器：

```sh
node server.mjs
```

然后访问 `http://127.0.0.1:8781/?variant=D`。页面底部按钮以及键盘左右方向键都可以切换方案。
